@model MyCourseraWebApp.ViewModel.ViewUserCoursesViewModel

@{
    ViewBag.Title = "Course I'm Instructing";
    ViewBag.Summary = "These are the courses I am instructing";
}


@section HeaderContent
{
    <div class="courses-page-instructing-search-main-container">
        @{
            Html.RenderPartial("CoursesSearchControl", Model);
        }
    </div>
}

<div class="container-fluid user-courses-main-container courses-page-items courses-page-instructing-items">
    @{
        Html.RenderPartial("UserCourses", Model);
    }
</div>

@section scripts
{
    <script type="text/javascript">
        $(document).ready(function () {
            coursesController.searchConrolsMainContainerSelector = ".courses-page-instructing-search-main-container";
            coursesController.bindingMode = 3;
            coursesController.bindPagerButtons();
            coursesController.bindAttendActionButtons();
            coursesController.bindUnAttendActionButtons();
            coursesController.bindSearchControls();
        });
    </script>
}